# Decimal to 
decimal = int(input("Enter a decimal number : "))

print("to binary : (prefix is : 0b)", bin(decimal))
print("to octal : (prefix is : 0o)", oct(decimal))
print("to hexadecimal : (prefix is : 0x)", hex(decimal))

